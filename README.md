# mysql-mcp-server

GitHub repo for the **`mysql-mcp-server-ms`** PyPI package ([install with `uvx mysql-mcp-server-ms`](mysql_server/README.md)). PyPI name uses the `-ms` suffix because `mysql-mcp-server` was already taken.